[phases.build]
cmds = ["composer install --no-interaction --no-dev --optimize-autoloader", "npm install", "npm run build"]

[phases.start]
cmd = "php artisan serve --host=0.0.0.0 --port=$PORT"

[variables]
NODE_VERSION = "20"
